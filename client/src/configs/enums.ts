enum Gesture { 
    Rock = 1, 
    Paper, 
    Scissors 
};

enum Result { 
    Win = "won",
    Draw = "drawn",
    Loss = "lost"
};

enum CardType { 
    Player,
    Opponent
};

export { Gesture, Result, CardType }